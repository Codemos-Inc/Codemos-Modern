{
  "name": "codemos-modern",
  "displayName": "Codemos Modern",
  "publisher": "Codemos",
  "icon": "res/icon.png",
  "description": "Innovative, modern theme suite/hub for VS Code",
  "version": "2.0.0",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Codemos-Inc/codemos-modern"
  },
  "sponsor": {
    "url": "https://github.com/sponsors/EmrecanKaracayir"
  },
  "galleryBanner": {
    "color": "#171717",
    "theme": "dark"
  },
  "extensionKind": [
    "ui"
  ],
  "categories": [
    "Themes"
  ],
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": true
    }
  },
  "engines": {
    "vscode": "^1.81.0"
  },
  "activationEvents": [
    "*"
  ],
  "main": "./out/main.js",
  "contributes": {
    "commands": [
      {
        "command": "codemosModern.configure",
        "title": "Codemos Modern: Configure ⚙️"
      },
      {
        "command": "codemosModern.authenticate",
        "title": "Codemos Modern: Authenticate 🔗"
      }
    ],
    "themes": [
      {
        "label": "Codemos Modern (Dark)",
        "uiTheme": "vs-dark",
        "path": "./themes/Codemos Modern (Dark)-color-theme.json"
      },
      {
        "label": "Codemos Modern (Light)",
        "uiTheme": "vs",
        "path": "./themes/Codemos Modern (Light)-color-theme.json"
      }
    ],
    "configuration": [
      {
        "properties": {
          "codemosModern.auxiliaryThemeRegistries": {
            "order": 0,
            "type": "array",
            "default": [
              "Codemos-Inc/Auxiliary-Theme-Registry"
            ],
            "minItems": 1,
            "markdownDescription": "GitHub repositories that are auxiliary theme registries. Should be in format `<owner>/<repo>`. See [the docs](https://github.com/Codemos-Inc/Codemos-Modern#modularity) for reference.",
            "pattern": "^[a-zA-Z0-9-]+/[a-zA-Z0-9._-]+$",
            "patternErrorMessage": "Please enter repositories in the valid format. Example: Codemos-Inc/Auxiliary-Theme-Registry"
          }
        }
      },
      {
        "title": "Dark Variant",
        "properties": {
          "codemosModern.dark.auxiliaryUiTheme": {
            "order": 0,
            "type": [
              "string",
              "null"
            ],
            "default": null,
            "markdownDescription": "Auxiliary theme to be used as the UI theme for `Codemos Modern (Dark)`. Should be in format `<owner>/<repo>/<publisher>/<extension>/<theme>`. See [the docs](https://github.com/Codemos-Inc/Codemos-Modern#modularity) for reference.",
            "pattern": "^[a-zA-Z0-9-]+/[a-zA-Z0-9._-]+/.+/.+/.+$",
            "patternErrorMessage": "Please enter a valid auxiliary theme. Example: Codemos-Inc/Auxiliary-Theme-Registry/Microsoft/Defaults/Dark Modern"
          },
          "codemosModern.dark.design": {
            "order": 1,
            "type": "string",
            "default": "modern",
            "enum": [
              "modern",
              "minimal"
            ],
            "enumDescriptions": [
              "Modern's original design",
              "Minimalistic design"
            ],
            "markdownDescription": "Design to be used in `Codemos Modern (Dark)`. Will be discarded when `codemosModern.dark.auxiliaryUiTheme` is set. See [the docs](https://github.com/Codemos-Inc/Codemos-Modern#modularity) for reference."
          },
          "codemosModern.dark.accentColor": {
            "order": 2,
            "type": "string",
            "default": "#CB8569",
            "markdownDescription": "HEX color code defining the accent color for `Codemos Modern (Dark)`. Will be discarded if `codemosModern.dark.auxiliaryUiTheme` is set.",
            "pattern": "^#[0-9A-Fa-f]{6}$",
            "patternErrorMessage": "Please enter a valid HEX color code. Example: #CB8569"
          },
          "codemosModern.dark.adaptiveMode": {
            "order": 3,
            "type": "string",
            "default": "none",
            "enum": [
              "none",
              "gentle",
              "aggressive",
              "aggressive"
            ],
            "enumDescriptions": [
              "No accent color adaptation",
              "Gentle accent color adaptation",
              "Moderate accent color adaptation",
              "Aggressive accent color adaptation"
            ],
            "markdownDescription": "Adaptive mode to be used in `Codemos Modern (Dark)`. Will be discarded when `codemosModern.dark.auxiliaryUiTheme` is set. See [the docs](https://github.com/Codemos-Inc/Codemos-Modern#adaptability) for reference."
          },
          "codemosModern.dark.auxiliaryCodeTheme": {
            "order": 4,
            "type": [
              "string",
              "null"
            ],
            "default": null,
            "markdownDescription": "Auxiliary theme to be used as the code theme for `Codemos Modern (Dark)`. Should be in format `<owner>/<repo>/<publisher>/<extension>/<theme>`. See [the docs](https://github.com/Codemos-Inc/Codemos-Modern#modularity) for reference.",
            "pattern": "^[a-zA-Z0-9-]+/[a-zA-Z0-9._-]+/.+/.+/.+$",
            "patternErrorMessage": "Please enter a valid auxiliary theme. Example: Codemos-Inc/Auxiliary-Theme-Registry/Microsoft/Defaults/Dark Modern"
          }
        }
      },
      {
        "title": "Light Variant",
        "properties": {
          "codemosModern.light.auxiliaryUiTheme": {
            "order": 0,
            "type": [
              "string",
              "null"
            ],
            "default": null,
            "markdownDescription": "Auxiliary theme to be used as the UI theme for `Codemos Modern (Light)`. Should be in format `<owner>/<repo>/<publisher>/<extension>/<theme>`. See [the docs](https://github.com/Codemos-Inc/Codemos-Modern#modularity) for reference.",
            "pattern": "^[a-zA-Z0-9-]+/[a-zA-Z0-9._-]+/.+/.+/.+$",
            "patternErrorMessage": "Please enter a valid auxiliary theme. Example: Codemos-Inc/Auxiliary-Theme-Registry/Microsoft/Defaults/Light Modern"
          },
          "codemosModern.light.design": {
            "order": 1,
            "type": "string",
            "default": "modern",
            "enum": [
              "modern",
              "minimal"
            ],
            "enumDescriptions": [
              "Modern's original design",
              "Minimalistic design"
            ],
            "markdownDescription": "Design to be used in `Codemos Modern (Light)`. Will be discarded when `codemosModern.light.auxiliaryUiTheme` is set. See [the docs](https://github.com/Codemos-Inc/Codemos-Modern#modularity) for reference."
          },
          "codemosModern.light.accentColor": {
            "order": 2,
            "type": "string",
            "default": "#784F3F",
            "markdownDescription": "HEX color code defining the accent color for `Codemos Modern (Light)`. Will be discarded when `codemosModern.light.auxiliaryUiTheme` is set.",
            "pattern": "^#[0-9A-Fa-f]{6}$",
            "patternErrorMessage": "Please enter a valid HEX color code. Example: #784F3F"
          },
          "codemosModern.light.adaptiveMode": {
            "order": 3,
            "type": "string",
            "default": "none",
            "enum": [
              "none",
              "gentle",
              "moderate",
              "aggressive"
            ],
            "enumDescriptions": [
              "No accent color adaptation",
              "Gentle accent color adaptation",
              "Moderate accent color adaptation",
              "Aggressive accent color adaptation"
            ],
            "markdownDescription": "Adaptive mode to be used in `Codemos Modern (Light)`. Will be discarded when `codemosModern.light.auxiliaryUiTheme` is set. See [the docs](https://github.com/Codemos-Inc/Codemos-Modern#adaptability) for reference."
          },
          "codemosModern.light.auxiliaryCodeTheme": {
            "order": 4,
            "type": [
              "string",
              "null"
            ],
            "default": null,
            "markdownDescription": "Auxiliary theme to be used as the code theme for `Codemos Modern (Light)`. Should be in format `<owner>/<repo>/<publisher>/<extension>/<theme>`. See [the docs](https://github.com/Codemos-Inc/Codemos-Modern#modularity) for reference.",
            "pattern": "^[a-zA-Z0-9-]+/[a-zA-Z0-9._-]+/.+/.+/.+$",
            "patternErrorMessage": "Please enter a valid auxiliary theme. Example: Codemos-Inc/Auxiliary-Theme-Registry/Microsoft/Defaults/Light Modern"
          }
        }
      }
    ]
  },
  "devDependencies": {
    "@types/node": "^20.3.3",
    "@types/vscode": "^1.79.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "eslint": "^8.39.0",
    "eslint-config-prettier": "^9.0.0",
    "husky": "^8.0.3",
    "lint-staged": "^14.0.0",
    "prettier": "^3.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4"
  },
  "scripts": {
    "prepare": "husky install",
    "compile:theme": "ts-node src/hooks/compileThemes.ts",
    "compile:stateFile": "ts-node src/hooks/compileStateFile.ts",
    "compile": "tsc",
    "build": "npm run compile && npm run compile:theme && npm run compile:stateFile",
    "lint": "eslint src --ext ts && tsc --noEmit",
    "format": "prettier --write .",
    "clean": "rm -rf out && rm -rf data/.state.json && rm -rf data/cache/*",
    "prebuild": "npm run clean && npm run format && npm run lint",
    "debug": "DEBUG=1 npm run build"
  },
  "lint-staged": {
    "*.ts": "eslint --fix",
    "*.{ts,json,md}": "prettier --write"
  },
  "dependencies": {
    "@octokit/rest": "^20.0.1",
    "node-fetch": "^2.6.12"
  }
}

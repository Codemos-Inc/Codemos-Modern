import { ThemeContext } from "../../@types";
import { configureExtensionSettings } from "./other";

export const configureSettings = (themeContext: ThemeContext): void => {
  configureExtensionSettings(themeContext);
};
